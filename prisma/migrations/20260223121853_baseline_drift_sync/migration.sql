-- This migration baselines the drift between the migration history and the actual database.
-- The database already has these changes (safety models, telegram, truck/trailer fields, etc.).
-- This empty migration resolves the drift so future migrations can proceed.

-- Intentionally empty: database already matches schema for drifted changes.
