'use client';

// Trucks layout - uses FleetManagementSidebar from SideNavWrapper
export default function TrucksLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

