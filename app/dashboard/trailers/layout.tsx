'use client';

// Trailers layout - uses FleetManagementSidebar from SideNavWrapper
export default function TrailersLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

